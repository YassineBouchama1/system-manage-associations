import TitlePage from '@/components/ui/TitlePage';
import type { FC } from 'react';

interface pageProps {}

const page: FC<pageProps> = ({}) => {
        return (
          <>

            <div>edit</div>
          </>
        );
}
export default page;